applySpecVersion: 1
spec:
  configSync:
    enabled: false
    sourceFormat: unstructured
    syncRepo: https://github.com/600lyy/BootstapKCCFromCS
    syncBranch: main
    secretType: token
    policyDir: /
  policyController:
    enabled: false 
